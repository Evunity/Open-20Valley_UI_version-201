# Local environment variables for Open Valley.
# This file is for non-secret values only and should not be committed with secrets.
# This is a 100% client-side UI/UX project; no environment variables are required to run it.

# Optional: message returned by GET /api/ping (only relevant if running the optional Express server).
PING_MESSAGE="ping pong"
